#added coment from pi
from Tkinter import *


class atm_client:
	def __init__(self):
		self.base_window = Tk()
		self.base_window.title('ATM Machine')
		
		self.base_window.mainloop()


client = atm_client()
